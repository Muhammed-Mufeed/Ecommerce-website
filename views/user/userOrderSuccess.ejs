<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Order Success - PackSmart</title>
  <link rel="stylesheet" href="/user/css/profile-sidebar.css" /> <!-- Assuming you reuse some styles -->
  <!-- Add Confetti.js from CDN -->
  <script src="https://cdn.jsdelivr.net/npm/canvas-confetti@1.6.0/dist/confetti.browser.min.js"></script>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: 'Roboto', sans-serif;
    }

    body {
      background-color: #f9fafb;
      color: #333;
      display: flex;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
      overflow: hidden; /* Prevent scroll during celebration */
    }

    .success-container {
      background-color: #fff;
      border-radius: 15px;
      box-shadow: 0 8px 20px rgba(0, 0, 0, 0.15);
      padding: 40px;
      text-align: center;
      max-width: 500px;
      width: 90%;
      position: relative;
      animation: popIn 0.5s ease-out forwards;
    }

    /* Popup Animation */
    @keyframes popIn {
      0% {
        transform: scale(0.8);
        opacity: 0;
      }
      100% {
        transform: scale(1);
        opacity: 1;
      }
    }

    .success-icon {
      width: 150px;
      height: 150px;
      margin-bottom: 20px;
      animation: bounce 1s infinite;
    }

    /* Bounce Animation for Icon */
    @keyframes bounce {
      0%, 20%, 50%, 80%, 100% {
        transform: translateY(0);
      }
      40% {
        transform: translateY(-20px);
      }
      60% {
        transform: translateY(-10px);
      }
    }

    .success-title {
      font-size: 32px;
      font-weight: 700;
      color: #2d3748;
      margin-bottom: 10px;
      animation: fadeIn 1s ease-in;
    }

    .success-message {
      font-size: 16px;
      color: #64748b;
      margin-bottom: 30px;
      animation: fadeIn 1.2s ease-in;
    }

    .order-id {
      font-size: 14px;
      color: #6c757d;
      margin-bottom: 20px;
      animation: fadeIn 1.4s ease-in;
    }

    /* Fade In Animation */
    @keyframes fadeIn {
      0% {
        opacity: 0;
      }
      100% {
        opacity: 1;
      }
    }

    .btn-container {
      display: flex;
      gap: 15px;
      justify-content: center;
    }

    .btn {
      padding: 10px 20px;
      border-radius: 6px;
      font-size: 14px;
      font-weight: 500;
      text-decoration: none;
      color: #fff;
      transition: transform 0.2s ease, background-color 0.3s ease;
      border: none;
      cursor: pointer;
    }

    .btn:hover {
      transform: scale(1.05);
    }

    .btn-details {
      background-color: #ff6f61;
    }

    .btn-details:hover {
      background-color: #e5645a;
    }

    .btn-shop {
      background-color: #4a5568;
    }

    .btn-shop:hover {
      background-color: #2d3748;
    }

    /* Confetti Canvas */
    canvas {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      pointer-events: none; /* Allows clicking through confetti */
      z-index: 9999;
    }

    @media (max-width: 768px) {
      .success-container {
        padding: 20px;
        margin: 10px;
      }

      .success-icon {
        width: 100px;
        height: 100px;
      }

      .success-title {
        font-size: 24px;
      }

      .btn-container {
        flex-direction: column;
        gap: 10px;
      }

      .btn {
        width: 100%;
      }
    }
  </style>
</head>
<body>
  <div class="success-container">
    <img src="https://cdn-icons-png.flaticon.com/512/148/148767.png" alt="Success Icon" class="success-icon" />
    <h1 class="success-title">Thank You!</h1>
    <p class="success-message">Your order has been placed successfully. We'll notify you once it's shipped.</p>
    <p class="order-id">Order ID: <strong>#<%= orderId %></strong></p>
    <div class="btn-container">
      <a href="/userOrders/<%= orderId %>" class="btn btn-details">View Order Details</a>
      <a href="/userProducts" class="btn btn-shop">Continue Shopping</a>
    </div>
  </div>

  <script>
    // Confetti Celebration
    document.addEventListener('DOMContentLoaded', () => {
      confetti({
        particleCount: 100,
        spread: 70,
        origin: { y: 0.6 },
        colors: ['#ff6f61', '#4a5568', '#2d3748', '#e5645a'],
      });

      // Optional: Trigger confetti again after 1 second for extra celebration
      setTimeout(() => {
        confetti({
          particleCount: 50,
          spread: 50,
          origin: { y: 0.4 },
          colors: ['#ff6f61', '#4a5568'],
        });
      }, 1000);
    });
  </script>
</body>
</html>